package CodeUp.SixThousandthBasic.Zero;

public class BasicSynthesis6084 {
	// Python code
	// h, b, c, s = input().split()
	// h = float(h)
	// b = float(b)
	// c = float(c)
	// s = float(s)
	// m = h*b*c*s/8/1024/1024
	// print(format(m, ".1f"), 'MB')
}
